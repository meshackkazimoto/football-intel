{
  "name": "football-intel",
  "private": true,
  "packageManager": "bun@1.3.5",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*",
    "scripts/*"
  ],

  "overrides": {
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.0"
  },

  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "@types/bun": "latest"
  }
}